import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({

  text: {
    fontWeight: 'bold',
    fontSize: 18,
    color: '#000',
    textAlign: 'center',
  },
  container:{
    paddingTop:50,
  }
});
